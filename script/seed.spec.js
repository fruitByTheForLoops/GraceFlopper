'use strict'
/* global describe it */

const seed = require('./seed')
const {prices, quantities} = require('./randomPricesAndQuantities')

describe('seed script', () => {
  it('completes successfully', seed)

  it('generates random prices')

  it('generates random quantities')
})
